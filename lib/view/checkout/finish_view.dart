import 'package:flutter/material.dart';

class FinishView extends StatelessWidget {
  const FinishView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Finsh'),),
    );
  }
}
